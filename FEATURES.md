# Features

<!-- repository-creator:managed:start -->

## Role

プロダクトが何をするかと、その機能をどこまで充実させるかを扱う。コア機能、機能拡充、ユーザーフロー、状態、業務ルール、権限、データ、外部連携を対象とする。コンテンツは `CONTENT.md`、見せ方は `DESIGN.md` の責務とする。

## Goal

初回は中心価値を成立させるコア機能へ集中する。コア完成後は、完成した実物を基準に不足を評価し、継続利用に必要な機能群をまとめて拡充してプロダクトレベルへ育てる。最初から周辺機能へ拡散することも、一機能ずつ細切れに増やすことも目的にしない。

## Core Establishment

- 対象ユーザー、中心価値、主要フローを確認し、入口から結果までを成立させるコア機能群を定める。
- 保存、空、読込中、失敗、取消・復帰など、中心体験に不可欠な状態はコアの完成条件として扱う。
- 複数の周辺機能へ広く着手せず、主要フローを高品質に完成させる。
- 実際に主要フローを使用して完了条件を確認し、見かけ上動くだけの状態を完成としない。

## Product Expansion

- コア完成後に、初回利用、反復利用、継続利用までのフロー全体と現在の機能を改めて評価する。
- 必要なら実在する類似・隣接プロダクトを調査し、重大な欠落、利用中断の原因、継続利用に必要な能力を洗い出す。
- 機能候補を列挙し、ユーザー価値、中心体験との接続、品質、複雑化、維持負担で `採用`、`保留`、`見送り` に分ける。
- 次に目指す完成状態を定め、採用した関連機能群を一つの `Expansion Set` として選ぶ。
- `Expansion Set` を一機能や一テーマへ限定しない。AIが一度に高品質で統合・検証でき、次の完成状態へ意味のある進歩を作れる範囲を基準にする。
- 既存機能との接続、データ、状態、権限、失敗時の振る舞いまで完成させ、独立した機能の寄せ集めにしない。
- 実装後は主要フローと重要な失敗系を検証し、プロダクト全体を再評価する。次に採用すべき重要な候補があれば反復する。

## Records

- コード、API仕様、テスト、Issueで十分に表現される詳細を重複して書かない。
- 現在の成熟度、コアの完了条件、次の完成状態、現在の `Expansion Set`、保留・見送り理由、長く維持すべき判断だけを下に残す。

<!-- repository-creator:managed:end -->

## Project Feature Maturity

- Product / users / core outcome: コマンドライン未経験の日本語話者が、実環境を壊す不安なく基本コマンドを自分で入力し、現在地・一覧・移動・作成・閲覧を一連の操作として説明できるようになる。
- Current maturity: 初期コアと第2周Expansion Setが完成。全5レッスン・10課題、状態保存、エラー回復、段階開示、公開Pages、Desktop / Mobileの主要フローまで実装・検証済み。
- Core feature set / completion status:
  - ブラウザ内だけで動くBash風ターミナルと、復元可能な擬似ファイルシステムを実装済み。
  - `help`、`clear`、`pwd`、`ls`、`cd`、`mkdir`、`touch`、`cat` を許可リストで処理し、実OSコマンドは実行しない。
  - 相対パス、絶対パス、`.`、`..` を解決し、Lesson 2で `ls .` と絶対パスを実際に入力する。存在しないパス、型の違い、引数違い、未対応コマンドを日本語で説明する。
  - 現在地や生成物を使う状態ベース判定で、妥当な操作の組み合わせを許容する。
  - 入出力履歴、上下キーの履歴移動、明示的なEnter実行、`Ctrl+L` の画面クリアを実装済み。初期レッスンでは処理トレースを常時表示し、後半は必要時に展開する。
  - 観察ポイント → 使うコマンド → 入力例の3段階ヒントと、実行せず入力欄へ置くだけの補助を提供する。
  - 進捗、現在地、擬似ファイルシステムを `localStorage` に保存し、レッスン単位と全体の確認付きリセットを提供する。
  - 全10課題を入口から最終ミッション完了まで通し、再読み込み後の復元も確認済み。
  - Desktopは説明と実習の36 / 64分割、Mobileは課題要約と下端詳細シートへ再構成する。Mobileの詳細シートからレッスン／全体リセットへ到達できる。
- Verification evidence: 2026-08-28に第2周として全10課題、旧パス入力の未達判定、`.`／絶対パスの合格判定、Enter実行、後半トレース再表示、代表エラー、保存復元、Desktop／Mobileのリセット、完了振り返りを実ブラウザで確認。Desktop 1280 × 720、Mobile 390 × 844で独立Visual QAを完了。
- Next target state: 初心者による短時間の実利用で、用語理解、課題文の迷い、エラー後の回復、Mobileキーボード表示時の操作を観察し、次のExpansion Setを決める。
- Expansion Set: 「学習カバレッジ＋公開品質」を完了。`.` と絶対パスの実課題、後半トレースの段階開示、完了振り返り、進捗アクセシビリティ、Mobile回復経路、共有メタデータ、GitHub Pages公開を一体で追加した。次のSetは実利用の証拠が得られるまで未設定。
- Deferred or rejected items / reasons:
  - 実Linux環境・コンテナ・VM: バックエンド、隔離、費用、待ち時間が必要になり、最初の学習価値に対して過大。
  - 認証・クラウド同期・ランキング・共有: 一人で基本操作を身につける中心フローに不要。
  - AIチャット: 状態判定と段階ヒントで先に学習体験を評価し、費用・誤案内・依存を増やさない。
  - `rm`、権限、パイプ、リダイレクト、シェルスクリプト: 初回範囲では認知負荷が高く、基礎操作の定着後に再評価する。
- Important flows / states / data decisions:
  - Flow: レッスン一覧で進捗確認 → 目的確認 → コマンド入力 → 擬似実行 → 状態判定 → 解説／ヒント → 次課題 → コース完了。
  - States: 入力待ち、実行成功、実行エラー、課題正解、課題未達、ヒント表示、コース完了、レッスンリセット確認、全体リセット確認。
  - Data: 教材、課題条件、擬似ファイルシステム、セッション履歴、進捗をクライアント内で管理し、保存キーは `terminal-dojo:v2` とする。
- Durable feature decisions: 実コマンドを実行しない。判定は可能な限り状態ベースにする。初心者の誤操作を学習材料として扱い、エラー後も実行記録と環境を維持する。入力は上下キーで復元できる。
